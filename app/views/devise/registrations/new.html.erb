<div class="bg">
  <div class="box">
    <div class="box-inner">
      <%= form_with model: @user, url_registration_path, id: 'new_user', class: 'new_user', local:true do |f| %>
      <%= render "devise/shared/error_messages", resource: resource %>
      <h1>新規登録</h1>
      <div class="box-name">
        <%= f.label :name, "名前" %><br />   